# Splitwise API Credentials
SPLITWISE_CLIENT_ID=your_client_id_here
SPLITWISE_CLIENT_SECRET=your_client_secret_here
SPLITWISE_GROUP_ID=your_group_id_here

# Wolt Authentication (optional - uses Chrome profile if not set)
WOLT_AUTH_BEARER_TOKEN=

# Email Notifications (optional - leave SMTP_USERNAME/PASSWORD empty to disable)
SMTP_SERVER=smtp.example.com
SMTP_PORT=465
SMTP_SSL=true
SMTP_AUTH=true
SMTP_USERNAME=
SMTP_PASSWORD=
MAIL_FROM=noreply@example.com
MAIL_TO=your-email@example.com
